{
  "name": "excel",
  "type": "module",
  "workspaces": [
    "lib",
    "sample"
  ],
  "scripts": {
    "update": "bun update.ts",
    "benchmark": "bun benchmark-tsconfig.ts",
    "build": "tsc --build",
    "build:clean": "tsc --build --clean",
    "build:force": "tsc --build --force"
  },
  "devDependencies": {
    "@types/bun": "^1.3.0",
    "ts-conf-base": "^2.4.2",
    "typescript": "^5.9.3",
    "execa": "^9.6.0"
  },
  "dependencies": {
    "lib": "^5.1.0",
    "sample": "^0.0.2"
  }
}
